apiVersion: batch/v1
kind: Job
metadata:
  name: simple-job
spec:
  template:
    spec:
      containers:
      - name: simple-job
        image: busybox
        command: ["sh", "-c", "echo Hello World!; sleep 50"]
      restartPolicy: Never